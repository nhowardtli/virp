# Definitive full-suite log — 2026-08-18 (Batch 6b + Batch 7)

Green full-suite run over the merged audit tree. The raw ~10k-line log is
kept out of this public repo; it is stored durably in the operator scratchpad
and pinned here by sha256 so this summary is verifiable against it.

## Tree under test

| | |
|---|---|
| Tested commit | `f200e621049600bbcfefc394c941b2976b1ab838` (Batch 7 branch tip) |
| Merged to main as | `d21b465d` |
| Branch base | `610c1670` (Batch 6b on main) |
| Date (UTC) | 2026-08-18T16:28:12Z |
| Host / user | virp-lab / nhoward (uid 1000, **not** root) |
| `VIRP_FED_SINCE` | 2026-08-18 (scopes the live-chain fed audits to the fixed daemon) |
| Compiler | gcc 13.3.0 (Ubuntu 24.04) |
| Python deps | fastapi 0.141.1, httpx 0.28.1, pytest 9.1.1, reportlab 4.1.0 |

## Targets and exit codes — all 0 (pass)

| Target | Exit | Notes |
|---|---|---|
| `VIRP_FED_SINCE=2026-08-18 make all-tests` | **0** | full battery; `check-test-deps` gate confirms no suite silently skipped |
| `make asan-test` | **0** | full C suite under ASan + UBSan — clean |
| `make asan-drivers` | **0** | driver / PBS byte-parser suites under ASan + UBSan — clean |

No `FAILED`, no sanitizer diagnostic, no `make *** Error` anywhere in the log.

## Representative per-suite results

| Suite | Result |
|---|---|
| test-onode (incl. 4 new GATE 5 commitment-only regressions) | 131/131 |
| test (core) | 182/182 |
| test-api (auth + bind-safety, ctypes bridge) | 246/246 |
| test-drivers | 33/33 |
| fed_outcome → observation pointer audit (live chain) | 4/4 |
| commitment-only observation grading (live chain) | 3/3 |
| v2 session observations / validator (live chain) | 44 + 50 |
| live-chain per-session verify (dominant runtime) | 47/47 (~2353 s) |

The four GATE 5 commitment-only regressions added this batch all pass:
`refuses_commitment_only_fed_retry_with_different_bytes`,
`concurrent_commitment_only_fed_conflict_is_atomic`,
`accepts_commitment_only_byte_identical_fed_retry`,
`non_fed_commitment_only_colliding_id_stores_both`.

## Raw log

- File: `all-tests-final-20260818T162812Z.log` (10,248 lines)
- sha256: `eebd8ccc4b6a240b1bfbe00dd45fcf015b3d9ba481c02f222a6fb2b9f53c6d20`
- Location: operator scratchpad (not committed — contains absolute build
  paths and live-chain artifact hashes). Recompute with `sha256sum` to
  confirm a copy matches this run.
