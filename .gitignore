onode-key.txt
devices_plain.json
devices.json
*.age
chain.db*
.env
*.key
build/
# driver-enabled test build tree (make test-drivers)
build-drivers/
# lab-only fault-injection build tree (make onode-fi) — never installed
build-fi/
__pycache__/
api/__pycache__/
tests/__pycache__/
*.tar.gz
*.bak
on
on.dump*
devices-r35.json
implementations/go/virp-onode-go
build-asan-drivers/

# Demo bundle outputs (regenerated by demo/run.sh)
demo/output/

# The demo target config is mock-only and safe to publish; it must survive
# the devices.json rule above, which exists to keep real credentials out.
!demo/devices.json

# libFuzzer build tree (make fuzz-obs-ed25519) — instrumented artifacts, never tracked
build-fuzz/

# Release bundles (make release-bundle / release.yml build outputs)
dist/
