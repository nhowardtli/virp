[Unit]
Description=VIRP Autopilot cross-node comparator every 10 minutes

[Timer]
# Offset from the :0/5 battery so the comparator reads a summary the
# battery has already published rather than racing it.
OnCalendar=*:2/10
AccuracySec=30s
Persistent=false

[Install]
WantedBy=timers.target
