# GENERATED BY deploy/tacacs/gen-green-conf.py -- DO NOT EDIT.
#
# Source of truth: src/drivers/driver_asa.c, the GREEN rows of
# its gate table. Regenerate rather than edit; an edit here
# makes the authorization server and the gate disagree about
# what GREEN means, and nothing would detect that.
#
# Match mode: driver longest-prefix
# GREEN commands: 39
#
# Each rule accepts an optional trailing ' <cr>' because that
# is how IOS terminates a per-command authorization request
# (cmd-arg=<cr>, reassembled by tac_plus-ng author.c). The
# terminator is OPTIONAL rather than required precisely so
# this file is correct on a platform that does not send it;
# whether the ASA does is a measurement, not an assumption,
# and the authorization log is where it gets settled.
#
# Slash delimiters select PCRE2. Double quotes would select
# POSIX ERE, which has no non-capturing group and rejects these.

    if (cmd =~ /\Ashow version(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow interface ip brief(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow interface brief(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow firewall(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow failover(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow conn count(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow route(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow clock(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow cpu usage(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow memory(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow xlate count(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow conn detail(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow conn(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow inventory(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow module(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow environment(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow process(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow nameif(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow interface(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow interfaces(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow ip address(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow ipv6 interface brief(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow arp(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow mac-address-table(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow switch vlan(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow xlate(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow local-host(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow uptime(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow blocks(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow traffic(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow perfmon(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow resource usage(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow processes(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow flash(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow disk0(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow file system(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow ospf(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow bgp(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }
    if (cmd =~ /\Ashow eigrp(?:[ ][^\x00-\x1f;`&]*)?(?: <cr>)?\z/) { permit }

